// Your code here...
#include <stdio.h>

int main() {
   int r, c, N, num;
   scanf("%d", &N);
   for (r = 1; r <= N; r++) {
    num= r
      for (c = 1; c <= r; c++) {
         printf("%d ",num);
      }
      printf("\n");
   }
   return 0;
}